spring:
  datasource:
    url: jdbc:h2:mem:typing;MODE=MySQL;DB_CLOSE_DELAY=-1;DATABASE_TO_LOWER=TRUE
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true
app:
  admin:
    password-hash: test-overridden
    token-secret: test-token-secret-at-least-32-characters
  allowed-origins: http://localhost:5173
